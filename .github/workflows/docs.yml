name: Documentation

on:
  pull_request:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Also publish an older stable release (vX.Y.Z); leave empty for latest and main'
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: documentation-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    name: Build documentation
    uses: hubuum/.github/.github/workflows/docs-build.yml@c026918decd83c681fedf0c4c61c266284ae4020 # shared documentation platform
    with:
      tooling_revision: 'c026918decd83c681fedf0c4c61c266284ae4020'
      source_ref: ${{ github.event_name == 'release' && 'main' || github.sha }}
      versioned: true
      python_api: true
      version: ${{ inputs.version || (github.event_name == 'release' && !github.event.release.prerelease && github.event.release.tag_name) || '' }}

  check:
    name: Documentation check
    needs: build
    if: always() && needs.build.result != 'skipped'
    runs-on: ubuntu-24.04
    steps:
      - env:
          BUILD: ${{ needs.build.result }}
        run: test "$BUILD" = success

  publish:
    name: Publish documentation
    needs: [build, check]
    if: >-
      github.repository == 'hubuum/hubuum-client-python' &&
      ((github.ref == 'refs/heads/main' && github.event_name != 'pull_request') ||
      (github.event_name == 'release' && !github.event.release.prerelease))
    permissions:
      contents: write
      pages: write
      id-token: write
    uses: hubuum/.github/.github/workflows/docs-publish.yml@c026918decd83c681fedf0c4c61c266284ae4020 # shared documentation platform
    with:
      tooling_revision: 'c026918decd83c681fedf0c4c61c266284ae4020'
      source_sha: ${{ needs.build.outputs.source_sha }}
